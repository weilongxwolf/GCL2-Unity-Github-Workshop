using UnityEngine;

public class MysteryController : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Mystery Box activated");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
